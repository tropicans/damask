---
phase: 02-masking-engine-download
plan: "03"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - backend/app/api/endpoints/mask.py
  - backend/app/api/api.py
autonomous: true
requirements:
  - FILE-04
user_setup: []

must_haves:
  primary_goal: "Provide file generation, streaming download, and transactional fail-fast boundaries."
  truths:
    - "StreamingResponse streams the file to client in-memory."
    - "Errors fail fast, raise HTTP 400/500, and clear memory buffers."
---

<objective>
Implement the backend masking endpoint, stream the file in its original format, and ensure Transactional Fail-fast.
</objective>

<tasks>
<task type="auto">
  <name>Create POST /api/mask endpoint and register it</name>
  <files>backend/app/api/endpoints/mask.py, backend/app/api/api.py</files>
  <action>
    Create endpoint `mask.py` implementing file processing, validation, masking application, and stream download with `_masked` suffix. Mount router in `api.py`.
  </action>
  <verify>
    Upload a CSV file and verify that the endpoint returns a correctly masked output and original layout.
  </verify>
  <done>Backend masking execution router is active and streams masked files correctly.</done>
</task>
</tasks>
