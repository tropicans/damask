---
phase: 05-documentation-suite
plan: 02
type: execute
wave: 1
depends_on:
  - "05-01"
files_modified:
  - docs/ARCHITECTURE.md
  - docs/API-SPEC.md
autonomous: true
requirements:
  - DOC-03
  - DOC-04
must_haves:
  truths:
    - "docs/ARCHITECTURE.md is created covering patterns, layers, and masking flows"
    - "docs/API-SPEC.md is created documenting all API endpoints and schemas"
  artifacts:
    - path: "docs/ARCHITECTURE.md"
      provides: "Architectural overview, data flows, and design patterns used"
    - path: "docs/API-SPEC.md"
      provides: "Complete API specification for endpoints and request/response models"
---

<objective>
Document the system's architecture and design patterns in /docs/ARCHITECTURE.md and specify all FastAPI endpoints in /docs/API-SPEC.md.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/app/main.py
@backend/app/api/endpoints/auth.py
@backend/app/api/endpoints/preview.py
@backend/app/api/endpoints/mask.py
@backend/app/api/endpoints/jobs.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/ARCHITECTURE.md</name>
  <files>docs/ARCHITECTURE.md</files>
  <read_first>backend/app/services/masker.py, backend/app/services/detector.py</read_first>
  <action>
    Create `docs/ARCHITECTURE.md` containing:
    - System design patterns (separation of concerns, FastAPI stateless API, React SPA).
    - Data masking workflows (in-memory buffering, stream processing, zero local file storage).
    - Design pattern application (Strategy pattern for different masking types: Fake Name, Fake Email, Fake Phone, Perturb Numeric, etc.).
    - Database layout / relationships (referencing users, masking_jobs, and job_details).
    Write in mixed Indonesian and English.
  </action>
  <verify>Check that docs/ARCHITECTURE.md exists and is formatted in Markdown.</verify>
  <acceptance_criteria>
    - `docs/ARCHITECTURE.md` exists.
    - Mentions memory processing, Strategy pattern, and database models.
  </acceptance_criteria>
  <done>docs/ARCHITECTURE.md written.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/API-SPEC.md</name>
  <files>docs/API-SPEC.md</files>
  <read_first>backend/app/api/endpoints/auth.py, backend/app/api/endpoints/preview.py, backend/app/api/endpoints/mask.py, backend/app/api/endpoints/jobs.py</read_first>
  <action>
    Create `docs/API-SPEC.md` documenting:
    - Base URL details.
    - Authentication endpoints (`POST /api/auth/register`, `POST /api/auth/login`, `GET /api/auth/me`).
    - File preview endpoint (`POST /api/preview/preview`).
    - File masking endpoint (`POST /api/mask/mask`).
    - Jobs and history endpoints (`GET /api/jobs/`, `GET /api/jobs/stats`, `GET /api/jobs/{job_id}/details`).
    For each endpoint, include the HTTP method, request headers (e.g. Authorization bearer), payload schema, and successful/failed JSON response structures.
    Write in mixed Indonesian and English.
  </action>
  <verify>Check that docs/API-SPEC.md exists and contains schemas for all endpoints.</verify>
  <acceptance_criteria>
    - `docs/API-SPEC.md` exists.
    - Contains descriptions of auth, preview, mask, and jobs endpoints.
  </acceptance_criteria>
  <done>docs/API-SPEC.md written.</done>
</task>

</tasks>

<verification>
- Check Markdown structure and readability of docs/ARCHITECTURE.md and docs/API-SPEC.md.
</verification>

<success_criteria>
- Technical docs docs/ARCHITECTURE.md and docs/API-SPEC.md are fully created.
- API models and schemas match the active Python code models.
</success_criteria>

<output>
Create `.planning/phases/05-documentation-suite/05-02-SUMMARY.md` on completion.
</output>
