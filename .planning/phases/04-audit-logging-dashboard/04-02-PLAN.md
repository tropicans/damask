---
phase: 04-audit-logging-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - frontend/src/api/jobs.ts
  - frontend/src/components/AuditDashboard.tsx
  - frontend/src/App.tsx
autonomous: false
requirements:
  - AUDT-03
---

<objective>
Implement frontend dashboard components and services to display the job execution history, summary cards, and job details modal.

Purpose: Allow users to visualize their masking history and check exactly which columns were sanitized with what rules.
Output: Axios client bindings, AuditDashboard component, and App.tsx navigation changes.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
@.agents/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Axios client functions for jobs API</name>
  <files>frontend/src/api/jobs.ts</files>
  <read_first>frontend/src/api/auth.ts, frontend/src/api/client.ts</read_first>
  <action>
    Create `frontend/src/api/jobs.ts` defining:
    - Interfaces `MaskingJobResponse`, `JobDetailResponse`, `JobStatsResponse`.
    - `getJobs(skip?, limit?)`: calls GET `/api/jobs` and returns array of jobs.
    - `getJobStats()`: calls GET `/api/jobs/stats` and returns stats.
    - `getJobDetails(jobId)`: calls GET `/api/jobs/${jobId}/details` and returns details list.
    Ensure API calls are authenticated using the shared token from localStorage via client headers.
  </action>
  <verify>Compile checks on frontend using `npm run build` or similar.</verify>
  <acceptance_criteria>
    - frontend/src/api/jobs.ts contains getJobs, getJobStats, and getJobDetails functions
    - Calls use the configured Axios client
  </acceptance_criteria>
  <done>API client is ready to make network requests to jobs backend.</done>
</task>

<task type="auto">
  <name>Task 2: Build AuditDashboard Component</name>
  <files>frontend/src/components/AuditDashboard.tsx</files>
  <read_first>frontend/src/components/AuthForm.tsx, frontend/src/App.tsx</read_first>
  <action>
    Create a new component `frontend/src/components/AuditDashboard.tsx` displaying:
    1. **Cards (Statistik Ringkasan)**:
       - Total Berkas Diproses (sukses)
       - Total Baris Disanitasi (sukses)
       - Rasio Keberhasilan (sukses/total, formatted as percentage)
    2. **Table (Riwayat Pekerjaan)**:
       - Columns: Waktu, Nama Berkas, Ukuran, Jumlah Baris, Status, Aksi.
       - Render a colored Badge for status: Green for "SUCCESS", Red for "FAILED".
       - Pagination buttons (Next/Previous) to browse history.
    3. **Detail Modal**:
       - Displays column names and rules applied for the job.
       - Displays error message if the job failed.
       - Opens smoothly on click of the "Detail" button.
    Apply consistent styling matching the dark glassmorphism theme from `App.tsx` and using Lucide icons.
  </action>
  <verify>Ensure compilation passes and CSS imports are valid.</verify>
  <acceptance_criteria>
    - frontend/src/components/AuditDashboard.tsx imports jobs API functions
    - AuditDashboard displays 3 summary cards, a paginated table, and a detail modal
    - Styling matches the dark UI aesthetic of the rest of the application
  </acceptance_criteria>
  <done>AuditDashboard component is complete and styled.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Tab Navigation in App.tsx</name>
  <files>frontend/src/App.tsx</files>
  <read_first>frontend/src/App.tsx</read_first>
  <action>
    Modify `frontend/src/App.tsx` to integrate tab navigation:
    - Add state `const [activeTab, setActiveTab] = useState<'masking' | 'audit'>('masking');`
    - In header, display two buttons for tab switching next to or below the project title:
      - "Masking Engine" (Lucide icon: FileUp or similar)
      - "Riwayat Audit" (Lucide icon: History or ClipboardList)
    - Conditionally render main view:
      - If `activeTab === 'masking'`, render the original Masking upload/preview flow.
      - If `activeTab === 'audit'`, render `<AuditDashboard />`.
    - Ensure when logging out, state is cleared and tab goes back to `'masking'`.
  </action>
  <verify>Ensure npm run build passes on the frontend.</verify>
  <acceptance_criteria>
    - App.tsx renders navigation tabs in header
    - Switching tabs mounts AuditDashboard or Masking Engine respectively
  </acceptance_criteria>
  <done>Frontend routing/tabs integration is complete.</done>
</task>

<task type="auto">
  <name>Start frontend dev server</name>
  <action>Run `npm run dev` in frontend, wait for ready</action>
  <verify>Check console or log for local address.</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Audit Dashboard UI integrated into App.tsx</what-built>
  <how-to-verify>
    1. Log in to the application.
    2. Click on the "Riwayat Audit" tab in the header.
    3. Verify that summary cards are rendered (Total Berkas, Total Baris, Rasio Keberhasilan).
    4. Perform a file masking on "Masking Engine" tab, download it, and switch back to "Riwayat Audit" tab.
    5. Verify the new log entry exists in the table.
    6. Click "Detail" on the successful job and check that the columns and their rules match the masking rules chosen.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `npm run build` in the frontend directory and check for TypeScript or bundler errors.
- [ ] Verify that navigating tabs works smoothly with visual active indicators.
</verification>

<success_criteria>
- All tasks completed.
- User can view dashboard statistics and history list.
- Column mapping details are shown in modal popups for audited files.
</success_criteria>

## Artifacts this phase produces

- **New Files**:
  - `frontend/src/api/jobs.ts`
  - `frontend/src/components/AuditDashboard.tsx`
- **New Types/Interfaces**:
  - `MaskingJobResponse` (in `frontend/src/api/jobs.ts`)
  - `JobDetailResponse` (in `frontend/src/api/jobs.ts`)
  - `JobStatsResponse` (in `frontend/src/api/jobs.ts`)
- **New Components**:
  - `AuditDashboard` (in `frontend/src/components/AuditDashboard.tsx`)
- **New Functions**:
  - `getJobs` (in `frontend/src/api/jobs.ts`)
  - `getJobStats` (in `frontend/src/api/jobs.ts`)
  - `getJobDetails` (in `frontend/src/api/jobs.ts`)

<output>
After completion, create `.planning/phases/04-audit-logging-dashboard/04-02-SUMMARY.md`
</output>

