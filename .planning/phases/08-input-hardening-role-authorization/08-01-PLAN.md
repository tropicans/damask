# Plan 08-01: Hardening Input File Validation

## Proposed Changes

### Backend: upload endpoints validation

#### [MODIFY] [preview.py](file:///c:/Users/X1%20Carbon/Downloads/Projects/self-hosted-ai-starter-kit/Dev/datamask/backend/app/api/endpoints/preview.py)
- Update `get_file_preview` endpoint:
  - Check the `Content-Length` header in `request.headers` first; reject if size > 50MB.
  - Check file `content_type` against valid CSV/Excel MIME-types.
  - Read incoming file in 8KB chunks; reject if cumulative size exceeds 50MB.

#### [MODIFY] [mask.py](file:///c:/Users/X1%20Carbon/Downloads/Projects/self-hosted-ai-starter-kit/Dev/datamask/backend/app/api/endpoints/mask.py)
- Update `mask_file` endpoint:
  - Check the `Content-Length` header in `request.headers` first; reject if size > 50MB.
  - Check file `content_type` against valid CSV/Excel MIME-types.
  - Read incoming file in 8KB chunks; reject if cumulative size exceeds 50MB.

## Verification Plan

### Automated Tests
- Create a test in `backend/app/tests/test_input_hardening.py` to upload a file with invalid MIME type (e.g. `image/png`) and verify it gets rejected with status code `400 Bad Request`.
- Upload a file larger than 50MB and verify it gets rejected with `413 Payload Too Large`.
- Run tests:
  ```bash
  docker compose exec backend env PYTHONPATH=. pytest app/tests/test_input_hardening.py
  ```
