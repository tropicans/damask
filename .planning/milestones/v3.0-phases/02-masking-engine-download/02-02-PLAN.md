---
phase: 02-masking-engine-download
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/App.tsx
  - frontend/src/api/mask.ts
autonomous: true
requirements:
  - MASK-01
user_setup: []

must_haves:
  primary_goal: "Enable dropdown selectors to configure rules and a button to trigger masking."
  truths:
    - "Tombol Mulai Masking is enabled when preview is loaded."
    - "Rules selected are correctly passed in API requests."
---

<objective>
Update frontend components and add the masking API client, hook up the dropdown selectors to state, and enable the masking trigger button.
</objective>

<tasks>
<task type="auto">
  <name>Create masking API client and update App.tsx</name>
  <files>frontend/src/api/mask.ts, frontend/src/App.tsx</files>
  <action>
    Create mask.ts helper and update App.tsx to support state selection, button activation, and masking API triggers.
  </action>
  <verify>
    Ensure Vite build succeeds and UI elements render properly.
  </verify>
  <done>Frontend trigger button and rule state are fully functional.</done>
</task>
</tasks>
