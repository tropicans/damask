---
phase: 01-foundation-preview-engine
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - backend/config/regex_rules.json
  - backend/app/services/parser.py
  - backend/app/services/detector.py
  - backend/app/api/endpoints/preview.py
autonomous: true
requirements:
  - PREV-01
  - PREV-02
  - PREV-03
user_setup: []

must_haves:
  primary_goal: "Implement memory-optimized parsing of CSV/XLSX file headers and first 3 rows, along with regex-based automatic rule detection."
  truths:
    - "Parsing CSV files extracts exactly 3 rows of data or less (if file has fewer than 3 rows)"
    - "Parsing XLSX files using openpyxl in read-only mode extracts exactly 3 rows of data or less without loading the entire 50MB structure"
    - "System maps parsed column headers to masking rules (Fake Name, Fake Email, Fake Phone, No Masking) using configuration regexes in config/regex_rules.json"
  artifacts:
    - path: "backend/config/regex_rules.json"
      provides: "External configuration for regex rules"
      contains: "Fake Email"
    - path: "backend/app/services/parser.py"
      provides: "Memory-optimized lazy parser for CSV/XLSX"
      contains: "def parse_csv_preview"
    - path: "backend/app/services/detector.py"
      provides: "Regex column headers recommendations generator"
      contains: "def recommend_masking_rules"
---

<objective>
Implement preview data parsing and automatic recommendation engine logic on the backend.

Purpose: Provides the core intelligence that inspects user uploads and returns column information, sample data, and recommended masking strategies.
Output: Parsing services, regex rules JSON configuration, and regex detection module.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-preview-engine/01-CONTEXT.md
@.planning/phases/01-foundation-preview-engine/01-UI-SPEC.md
@.planning/phases/01-foundation-preview-engine/01-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Create regex rules config file</name>
  <files>backend/config/regex_rules.json</files>
  <action>
    Create a JSON file `backend/config/regex_rules.json` that contains standard mapping rules.
    Provide regex lists for:
    - "Fake Email": ["email", "mail", "surel", "pos-el"]
    - "Fake Name": ["name", "nama", "fullname", "contact"]
    - "Fake Phone": ["phone", "telp", "hp", "mobile", "telepon"]
    - "Anonymize ID/Number": ["id", "code", "nik", "no_", "nomor", "npwp"]
    - "Perturb Numeric": ["salary", "gaji", "price", "harga", "amount", "jumlah", "total", "revenue"]
  </action>
  <verify>
    Ensure file exists and contains valid JSON syntax.
  </verify>
  <acceptance_criteria>
    - regex_rules.json contains mappings for Fake Email, Fake Name, Fake Phone, Anonymize ID/Number, and Perturb Numeric.
  </acceptance_criteria>
  <done>Configuration file created with valid JSON structure.</done>
</task>

<task type="auto">
  <name>Implement CSV and XLSX lazy preview parsing service</name>
  <files>backend/app/services/parser.py</files>
  <read_first>backend/app/api/endpoints/preview.py</read_first>
  <action>
    Create `backend/app/services/parser.py` containing:
    - `parse_csv_preview(file_buffer: io.BytesIO) -> tuple[list[str], list[list]]`: parses the CSV file buffer using `pandas.read_csv(file_buffer, nrows=3, dtype=str)`. Wrap in try/except to fallback to `encoding='latin1'` on UnicodeDecodeError.
    - `parse_xlsx_preview(file_buffer: io.BytesIO) -> tuple[list[str], list[list]]`: uses `openpyxl.load_workbook(file_buffer, read_only=True, data_only=True)`. Read the active sheet, pull the first row as headers, and iterate `ws.iter_rows(values_only=True)` to get exactly 3 data rows. Close the workbook in a `finally` block to prevent file descriptor leaks.
  </action>
  <verify>
    Write a quick python verification check or run unit tests on mock ByteIO buffers to ensure headers and rows are correctly returned.
  </verify>
  <acceptance_criteria>
    - parser.py contains `parse_csv_preview` and `parse_xlsx_preview`.
    - `parse_xlsx_preview` uses `read_only=True` and explicitly calls `wb.close()`.
    - Both functions return a list of headers and up to 3 list rows.
  </acceptance_criteria>
  <done>In-memory lazy parsing service implemented successfully.</done>
</task>

<task type="auto">
  <name>Implement column recommendation engine and wire to API</name>
  <files>backend/app/services/detector.py, backend/app/api/endpoints/preview.py</files>
  <read_first>backend/config/regex_rules.json, backend/app/services/parser.py, backend/app/api/endpoints/preview.py</read_first>
  <action>
    Create `backend/app/services/detector.py` containing:
    - `recommend_masking_rules(headers: list[str], rules_config_path: str) -> dict[str, str]`: reads `regex_rules.json`, iterates through headers, normalizes names to lowercase, runs regex searches, and maps matches to masking rules. If no match is found, recommends "No Masking".
    Update `backend/app/api/endpoints/preview.py` to:
    - Seek the file buffer back to 0.
    - Call the parser service depending on file extension to extract headers and preview rows.
    - Call the recommendation engine with headers and `backend/config/regex_rules.json` path.
    - Return a JSON response structured as: `{"filename": str, "headers": list, "preview_rows": list, "recommendations": dict}`.
  </action>
  <verify>
    Upload a test CSV file containing columns like "Nama Lengkap, Email_Toko, Jumlah_Gaji, Alamat" using curl and verify the JSON output contains correct recommended rules: "Fake Name", "Fake Email", "Perturb Numeric", "No Masking" respectively.
  </verify>
  <acceptance_criteria>
    - detector.py loads and compiles patterns from config.
    - API returns headers, preview_rows, and recommendations dict.
  </acceptance_criteria>
  <done>Preview endpoint fully wired and recommends rules successfully based on regex configuration.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] Run backend local server.
- [ ] Send POST request to `/api/preview` with CSV containing columns "nama_lengkap, email, no_hp, gaji" and verify the recommendations field is populated with Fake Name, Fake Email, Fake Phone, Perturb Numeric.
- [ ] Send POST request with XLSX file and verify same output.
- [ ] Verify that no files are written to backend directory.
</verification>

<success_criteria>
- All tasks completed.
- Backend successfully parses CSV/XLSX preview rows in-memory.
- Automatic column recommendation maps correctly to rules in config.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-preview-engine/01-02-SUMMARY.md`
</output>
