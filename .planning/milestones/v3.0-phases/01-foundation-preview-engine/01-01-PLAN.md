---
phase: 01-foundation-preview-engine
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/main.py
  - backend/app/core/config.py
  - backend/app/api/api.py
  - backend/app/api/endpoints/preview.py
  - docker-compose.yml
  - backend/Dockerfile
autonomous: true
requirements:
  - FILE-01
  - FILE-02
  - FILE-03
user_setup: []

must_haves:
  primary_goal: "Setup the FastAPI backend scaffold and establish the file upload endpoint accepting CSV/XLSX files in-memory."
  truths:
    - "FastAPI backend runs and responds to GET /health with a JSON status success"
    - "FastAPI backend handles POST /api/preview multipart/form-data upload of CSV and XLSX files under 50MB and rejects larger payloads with HTTP 413"
    - "The endpoint parses the upload entirely in-memory using io.BytesIO without writing to the disk"
  artifacts:
    - path: "backend/app/main.py"
      provides: "FastAPI server initialization and health router"
      contains: "FastAPI"
    - path: "backend/app/api/endpoints/preview.py"
      provides: "Endpoint for previewing and checking uploaded files"
      contains: "router = APIRouter()"
    - path: "docker-compose.yml"
      provides: "Docker orchestration config for frontend and backend"
      contains: "services:"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/api/api.py"
      via: "includes api_router"
      pattern: "include_router"
    - from: "backend/app/api/api.py"
      to: "backend/app/api/endpoints/preview.py"
      via: "includes preview router"
      pattern: "include_router.*preview"
---

<objective>
Set up the FastAPI backend scaffold with Uvicorn, Docker Compose configuration, and implement the initial in-memory file upload endpoint.

Purpose: Establishes the server-side foundation and handles memory-only file processing before we add parsing and preview logic.
Output: Backend python app files, Dockerfile, docker-compose.yml, and an upload REST API endpoint.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-preview-engine/01-CONTEXT.md
@.planning/phases/01-foundation-preview-engine/01-UI-SPEC.md
</context>

<tasks>
<task type="auto">
  <name>Scaffold backend python app config and Dockerfile</name>
  <files>backend/pyproject.toml, backend/Dockerfile, docker-compose.yml</files>
  <action>
    Create backend directory structures. Write `backend/pyproject.toml` with poetry dependencies: fastapi, uvicorn, python-multipart, pandas, openpyxl, and pydantic-settings.
    Write `backend/Dockerfile` using Python 3.11-slim, installing dependencies, and exposing port 8000.
    Write `docker-compose.yml` in the root workspace defining 'backend' service mapping port 8000:8000 and volume-mounting the backend files for local development.
  </action>
  <verify>
    Run `docker compose build backend` to check if the backend Docker image builds successfully.
  </verify>
  <acceptance_criteria>
    - backend/pyproject.toml contains dependencies: fastapi, uvicorn, python-multipart, pandas, openpyxl.
    - docker-compose.yml contains services.backend mapping port 8000.
  </acceptance_criteria>
  <done>Docker build succeeds for backend service.</done>
</task>

<task type="auto">
  <name>Implement FastAPI server core, config, and logging</name>
  <files>backend/app/main.py, backend/app/core/config.py, backend/app/core/logging.py</files>
  <action>
    Create `backend/app/core/config.py` using Pydantic Settings to read CORS allowed origins.
    Create `backend/app/core/logging.py` configuring standard python logging outputting to stdout.
    Create `backend/app/main.py` initializing FastAPI, setting up CORS middleware, logging startup metadata, and adding a `GET /health` router.
  </action>
  <verify>
    Spin up backend locally or via docker-compose and query `curl http://localhost:8000/health`.
  </verify>
  <acceptance_criteria>
    - main.py initializes FastAPI and adds CORSMiddleware.
    - /health responds with status success.
  </acceptance_criteria>
  <done>Backend FastAPI server starts up and responds to health checks.</done>
</task>

<task type="auto">
  <name>Create POST /api/preview file upload router</name>
  <files>backend/app/api/api.py, backend/app/api/endpoints/preview.py</files>
  <action>
    Create `backend/app/api/endpoints/preview.py` with `POST /api/preview` endpoint accepting `file: UploadFile = File(...)`.
    Implement file extension verification: only `.csv` and `.xlsx` are allowed.
    Implement RAM-only byte extraction: read all bytes asynchronously with `await file.read()`.
    Implement 50MB file size limit check on the read bytes. If size > 50MB, raise HTTPException with status 413 (Payload Too Large).
    Create `backend/app/api/api.py` combining routers and including the preview router with prefix `/api`. Import and include this router in `backend/app/main.py`.
  </action>
  <verify>
    Start the server and upload a dummy .csv file using curl: `curl -F "file=@dummy.csv" http://localhost:8000/api/preview`. Verify it returns file metadata (filename, size).
  </verify>
  <acceptance_criteria>
    - preview.py limits file size to 50MB (52428800 bytes).
    - preview.py validates file endings.
    - preview.py reads data in memory and wraps in BytesIO buffer.
  </acceptance_criteria>
  <done>FastAPI endpoint accepts CSV/XLSX uploads, verifies format and size constraints in-memory.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] Backend Docker image builds and starts up successfully via `docker compose up -d backend`.
- [ ] curl request to http://localhost:8000/health returns status success.
- [ ] Test file upload via curl with a small .csv file; endpoint should return 200 OK with file metadata.
- [ ] Test file upload with a >50MB dummy file; endpoint should return 413 Payload Too Large.
</verification>

<success_criteria>
- All tasks completed.
- FastAPI backend runs and serves API routes.
- File uploads are validated and read entirely into memory buffers without disk writes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-preview-engine/01-01-SUMMARY.md`
</output>
