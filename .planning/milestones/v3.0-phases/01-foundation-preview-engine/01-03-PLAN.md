---
phase: 01-foundation-preview-engine
plan: "03"
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tailwind.config.js
  - frontend/postcss.config.js
  - frontend/src/index.css
  - frontend/src/api/preview.ts
  - frontend/src/components/Dropzone.tsx
  - frontend/src/components/PreviewTable.tsx
  - frontend/src/App.tsx
autonomous: false
requirements:
  - FILE-01
  - PREV-01
  - PREV-02
user_setup: []

must_haves:
  primary_goal: "Implement React drag-and-drop dropzone, upload file preview table with Tailwind CSS styling, and integrate with backend api."
  truths:
    - "Frontend validates file size (< 50MB) and formats (.csv/.xlsx) on drag/selection and displays toast errors for invalid files"
    - "Frontend uploads file to backend preview API and handles loading state during upload"
    - "Frontend renders first 3 rows of data in a responsive table, with column dropdowns pre-selected with recommendations from backend"
  artifacts:
    - path: "frontend/src/components/Dropzone.tsx"
      provides: "Drag and drop container with visual drop feedback"
      contains: "onDragOver"
    - path: "frontend/src/components/PreviewTable.tsx"
      provides: "Grid preview of CSV/XLSX data with masking options"
      contains: "select"
    - path: "frontend/src/api/preview.ts"
      provides: "Axios client posting form data to backend"
      contains: "axios.post"
---

<objective>
Implement frontend React drag-and-drop file upload, preview data table, and wire to FastAPI backend API.

Purpose: Provides the visual layer for users to interactively drag files and see columns mapped, implementing Tailwind CSS styling.
Output: UI components, CSS files, Axios API service, and App view.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
@.agents/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-preview-engine/01-CONTEXT.md
@.planning/phases/01-foundation-preview-engine/01-UI-SPEC.md
</context>

<tasks>
<task type="auto">
  <name>Initialize frontend scaffold, packages, and TailwindCSS</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/tailwind.config.js, frontend/postcss.config.js, frontend/src/index.css</files>
  <action>
    Configure Vite React project in `frontend/`. Install TailwindCSS v3 and PostCSS dependencies.
    Write `frontend/tailwind.config.js` pointing to index.html and all source files in `src/`.
    Write `frontend/postcss.config.js` importing tailwindcss and autoprefixer.
    Write `frontend/src/index.css` importing `@tailwind base; @tailwind components; @tailwind utilities;` and define custom deep dark background color theme:
    - Dominant Dark: `#0b0f19`
    - Secondary Slate: `#161f30`
    - Accent Indigo: `#6366f1`
  </action>
  <verify>
    Ensure `npm run build` or `vite build` runs successfully in the frontend directory.
  </verify>
  <acceptance_criteria>
    - tailwind.config.js contains content configuration.
    - index.css imports tailwind layers.
  </acceptance_criteria>
  <done>Frontend project setup with TailwindCSS v3 complete.</done>
</task>

<task type="auto">
  <name>Implement Axios API and upload components</name>
  <files>frontend/src/api/preview.ts, frontend/src/components/Dropzone.tsx</files>
  <read_first>backend/app/api/endpoints/preview.py</read_first>
  <action>
    Create `frontend/src/api/preview.ts` creating an Axios instance and defining `uploadFileForPreview(file: File)` making a POST request to `http://localhost:8000/api/preview` with FormData.
    Create `frontend/src/components/Dropzone.tsx` rendering a premium Indigo-bordered dashed card. Implement dragOver, dragLeave, and drop handlers. Validates size < 50MB and format (.csv, .xlsx) before calling upload.
    Include visual hover effects (active border color change when dragging) and a loading spinner when uploading.
  </action>
  <verify>
    Ensure files import Axios and Lucide React icons correctly.
  </verify>
  <acceptance_criteria>
    - Dropzone.tsx includes drag-and-drop state listeners.
    - preview.ts targets `/api/preview`.
  </acceptance_criteria>
  <done>Dropzone and API upload service ready.</done>
</task>

<task type="auto">
  <name>Implement preview table UI and combine in App view</name>
  <files>frontend/src/components/PreviewTable.tsx, frontend/src/App.tsx</files>
  <read_first>frontend/src/components/Dropzone.tsx, frontend/src/index.css</read_first>
  <action>
    Create `frontend/src/components/PreviewTable.tsx` receiving headers, rows, and recommended rules from backend.
    Renders a responsive scrollable grid with headers and first 3 rows of data.
    Under each header cell, render a dropdown select box prefilled with the recommended rule from backend, matching rules: "No Masking", "Fake Name", "Fake Email", "Fake Phone", "Anonymize ID/Number", "Perturb Numeric".
    Update `frontend/src/App.tsx` coordinating the state: file upload status, loading status, preview data (headers, rows, recommendations). Render Title, Description, Dropzone, and PreviewTable in a modern centered layout with Slate/Indigo dark theme.
  </action>
  <verify>
    Ensure frontend builds without TypeScript compiler errors.
  </verify>
  <acceptance_criteria>
    - PreviewTable.tsx uses `overflow-x-auto` to handle column overflow.
    - Select dropdowns pre-select recommendations.
  </acceptance_criteria>
  <done>Frontend UI completed and wired to App container.</done>
</task>

<task type="auto">
  <name>Configure local execution environment</name>
  <files>docker-compose.yml</files>
  <read_first>docker-compose.yml</read_first>
  <action>
    Update `docker-compose.yml` to define the `frontend` service mapping port 5173 (Vite port) and referencing Vite development server. Set proxy or CORS env vars to allow cross-origin requests.
  </action>
  <verify>
    Run `docker compose build` to verify both services build successfully.
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains both frontend and backend services.
  </acceptance_criteria>
  <done>Docker-compose orchestration configured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>SecureData Web drag-and-drop preview dashboard. Services run at frontend: http://localhost:5173, backend: http://localhost:8000</what-built>
  <how-to-verify>
    Run `docker compose up` to start both frontend and backend services.
    Open http://localhost:5173. Verify:
    1. Dark mode layout with "SecureData Web" title.
    2. Drag and drop a valid CSV/XLSX file; dragover changes border to Indigo.
    3. File is uploaded; loading spinner is shown during upload.
    4. 3-row preview table appears with columns, sample rows, and rule dropdowns pre-selected based on column names.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] Both Docker containers run smoothly without crash loops.
- [ ] UI visual check shows sleek design matching 01-UI-SPEC.md color palette (#0b0f19 background, Indigo focus).
</verification>

<success_criteria>
- All tasks completed.
- Visual verification passed by user.
- Drag-and-drop and table preview are fully operational.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-preview-engine/01-03-SUMMARY.md`
</output>
