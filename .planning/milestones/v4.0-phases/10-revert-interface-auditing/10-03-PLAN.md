---
phase: 10-revert-interface-auditing
plan: "10-03"
type: execute
wave: 3
depends_on:
  - "10-02"
files_modified:
  - backend/app/models/job.py
  - backend/app/db.py
  - backend/app/api/endpoints/mask.py
  - backend/app/api/endpoints/jobs.py
  - frontend/src/api/jobs.ts
  - frontend/src/components/AuditDashboard.tsx
autonomous: true
requirements:
  - REV-05
  - REV-06
user_setup: []

must_haves:
  primary_goal: "Add RevertJob database model, implement audit logging inside the revert endpoint, expose revert log endpoints, and integrate revert logs into the Audit Dashboard UI."
  truths:
    - "D-04: Create a separate RevertJob SQLModel and DB table to track data reversion metadata (including id, user_id, file_name, file_size_bytes, row_count, execution_duration_ms, status, error_message, and created_at). Reversion operations do not have column-level rules to log."
    - "D-05: Present revert execution records inside the 'Riwayat Audit' tab by introducing a sub-navigation or toggle to switch between 'Riwayat Masking' (Masking History) and 'Riwayat Revert' (Reversion History)."
    - "D-06: Selecting detail info for a revert job in the dashboard displays a modal containing only metadata properties (File name, size, row count, status, created time, and execution duration in ms) with no column-rule tables."
  truths_ui:
    - "Uses the established dashboard layout, with an indigo sub-navigation selector for Masking vs Reversion."
  truths_security:
    - "Log retrieval routes require user role check (admin/auditor) and filter logs by user_id."
  artifacts:
    - path: "backend/app/models/job.py"
      provides: "RevertJob database model definition"
      contains: "RevertJob"
    - path: "backend/app/api/endpoints/mask.py"
      provides: "Audit logging in /api/mask/revert endpoint"
      contains: "revert_file"
    - path: "backend/app/api/endpoints/jobs.py"
      provides: "Retrieval endpoints for revert logs and statistics"
      contains: "get_revert_jobs"
    - path: "frontend/src/api/jobs.ts"
      provides: "Client API handlers for revert logs"
      contains: "getRevertJobs"
    - path: "frontend/src/components/AuditDashboard.tsx"
      provides: "Sub-navigation and reversion log visual elements"
      contains: "AuditDashboard"
  key_links: []
---

<objective>
Implement backend revert auditing, write log records for every reversion operation, expose retrieve API endpoints, and update the React Audit Dashboard to display reversion history statistics and list records.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-revert-interface-auditing/10-CONTEXT.md
@.planning/phases/10-revert-interface-auditing/10-UI-SPEC.md
</context>

<threat_model>
### Security Threat Model (ASVS L1)
- **Threat**: Unauthorized users accessing data reversion logs.
  - **Mitigation**: Backend logs query endpoints validate that `current_user.role` belongs to `admin` or `auditor` and raise a 403 Forbidden on failure.
</threat_model>

<tasks>
<task type="auto">
  <name>Define RevertJob model in backend</name>
  <files>backend/app/models/job.py</files>
  <action>
    Add `RevertJob` class to `backend/app/models/job.py`:
      - Fields: `id` (primary key UUID), `user_id` (foreign key to users.id), `file_name` (string), `file_size_bytes` (integer), `row_count` (optional integer), `execution_duration_ms` (integer), `status` (string: "SUCCESS" or "FAILED"), `error_message` (optional string), and `created_at` (datetime default utcnow).
      - Table name: `revert_jobs`.
  </action>
  <verify>
    Verify python syntax and model import.
  </verify>
  <acceptance_criteria>
    - `RevertJob` table is defined.
    - Fields match the D-04 database schema.
  </acceptance_criteria>
  <done>RevertJob database model is defined.</done>
</task>

<task type="auto">
  <name>Import RevertJob in db.py</name>
  <files>backend/app/db.py</files>
  <action>
    Modify `init_db` function in `backend/app/db.py`:
      - Import `RevertJob` from `app.models.job` alongside `MaskingJob, JobDetail` to register it under metadata.
  </action>
  <verify>
    Verify imports are correct and run `init_db()` without exceptions.
  </verify>
  <acceptance_criteria>
    - `RevertJob` is imported in `db.py`.
  </acceptance_criteria>
  <done>RevertJob registration is added to database initialization.</done>
</task>

<task type="auto">
  <name>Implement logging inside /api/mask/revert endpoint</name>
  <files>backend/app/api/endpoints/mask.py</files>
  <action>
    Update `/api/mask/revert` in `backend/app/api/endpoints/mask.py`:
      - Measure the execution duration of the reversion operation using `time.perf_counter()`.
      - Wrap the file parsing, reversion, and formatting in a try/except block.
      - Upon success: write a `RevertJob` record with status `"SUCCESS"`, the calculated `row_count`, and `execution_duration_ms`.
      - Upon failure/HTTPException: write a `RevertJob` record with status `"FAILED"`, `row_count=None` or whatever was processed, the error message, and duration.
  </action>
  <verify>
    Run tests to ensure reverting a file creates DB log records.
  </verify>
  <acceptance_criteria>
    - Every `/api/mask/revert` request logs a `RevertJob` record.
    - Duration and status are accurately recorded.
  </acceptance_criteria>
  <done>Audit log recording is added to reversion API endpoint.</done>
</task>

<task type="auto">
  <name>Implement revert log retrieval endpoints in backend</name>
  <files>backend/app/api/endpoints/jobs.py</files>
  <action>
    Modify `backend/app/api/endpoints/jobs.py`:
      - Import `RevertJob` from `app.models.job`.
      - Add endpoint `GET /revert` matching `get_jobs`:
        - Validates that user role is admin or auditor.
        - Queries and returns user's paginated `RevertJob` records ordered by `created_at` descending.
      - Add endpoint `GET /revert/stats` matching `get_jobs_stats`:
        - Validates user role.
        - Calculates total reverted files (SUCCESS), total reverted rows (SUCCESS), and success rate.
        - Returns a `JobStatsResponse`.
  </action>
  <verify>
    Verify backend routers compile and test endpoints manually or with pytest.
  </verify>
  <acceptance_criteria>
    - `GET /api/jobs/revert` and `GET /api/jobs/revert/stats` endpoints exist.
    - Enforce role security controls and ownership checks.
  </acceptance_criteria>
  <done>Audit logs API endpoints are exposed for reversion records.</done>
</task>

<task type="auto">
  <name>Extend frontend API client for reversion logs</name>
  <files>frontend/src/api/jobs.ts</files>
  <action>
    Modify `frontend/src/api/jobs.ts`:
      - Add `RevertJobResponse` interface (fields match RevertJob).
      - Add `getRevertJobs(skip, limit): Promise<RevertJobResponse[]>` fetching from `/api/jobs/revert`.
      - Add `getRevertJobStats(): Promise<JobStatsResponse>` fetching from `/api/jobs/revert/stats`.
  </action>
  <verify>
    Verify typescript compiles successfully.
  </verify>
  <acceptance_criteria>
    - API client functions exist and match backend endpoints.
  </acceptance_criteria>
  <done>Frontend client helpers for reversion audit logs are implemented.</done>
</task>

<task type="auto">
  <name>Update Audit Dashboard UI to show Reversion History</name>
  <files>frontend/src/components/AuditDashboard.tsx</files>
  <action>
    Modify `frontend/src/components/AuditDashboard.tsx`:
      - Add state for history tab: `historySubTab: 'masking' | 'revert'` (default `'masking'`).
      - In the UI (above stats cards or history table), add a toggle component (tabs/pills) to switch between "Riwayat Masking" and "Riwayat Revert".
      - Fetch either masking stats/logs or reversion stats/logs based on the selected `historySubTab` (triggering calls to `getRevertJobs` / `getRevertJobStats` or their masking equivalents).
      - Render stats cards and table based on the sub-tab selection:
        - When "Riwayat Revert" is selected, show reversion stats (Total Berkas Direvert, Total Baris Dipulihkan, Tingkat Keberhasilan).
        - Show reversion history table (File, Size, Rows, Duration, Status, Timestamp).
      - Modify detail modal logic:
        - Clicking a row in Revert history displays a details modal that only displays metadata (File name, size, row count, status, created time, and execution duration in ms) and does NOT render the column rules table (as reversion has no column-level rule metadata to query).
  </action>
  <verify>
    Verify dashboard compiles and switches tabs seamlessly. Inspect formatting of stats cards, log table, and the detail modal.
  </verify>
  <acceptance_criteria>
    - Sub-nav toggle allows switching history views.
    - Correct API calls are made and state updates on tab switch.
    - Detail modal for revert jobs does not show column details and displays duration.
  </acceptance_criteria>
  <done>Audit Dashboard UI is updated with Reversion History and details modal.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- Run `poetry run pytest` in `backend` to ensure backend compiles.
- Run `npm run build` in `frontend` to verify frontend builds successfully.
</verification>

<success_criteria>
- Revert executions log metadata records successfully.
- Revert history and statistics are listed in the Audit Dashboard for authorized users.
- Revert job details modal renders metadata without column rules.
</success_criteria>

<output>
After completion, create `.planning/phases/10-revert-interface-auditing/10-03-SUMMARY.md`
</output>
